import { tv } from "tailwind-variants";

export const fieldsetRecipe = tv({
  base: "fieldset bg-base-200 border border-base-300 p-4 rounded-box",
});

export const fieldsetLegendRecipe = tv({ base: "fieldset-legend text-lg" });

export const fieldsetLabelRecipe = tv({ base: "fieldset-label" });
